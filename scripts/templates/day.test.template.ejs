// Advent of Code - Day <%= data.dayNumber %>

import { part1, part2 } from '../src/<%= data.dayName %>';

test('part one test', () => {
  expect(part1("")).toBe(0);
});

test('part two test', () => {
  expect(part2("")).toBe(0);
});
